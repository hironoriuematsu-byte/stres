# ストレスチェックWebシステム 実運用版 実装指示書

Claude Codeへの指示: このリポジトリ(stres)の既存プロトタイプを、以下の仕様で実運用版に改修してください。データベーススキーマは同梱の `supabase/migrations/0002_production.sql` を正とし、これに合わせてフロントエンドを実装します。スキーマ側のRLSがアクセス制御の最終防衛線なので、フロント側で権限チェックを重複実装しても構いませんが、RLSを緩めるスキーマ変更は行わないでください。

## 0. 前提・技術構成

- React (Vite) + Supabase JS Client v2 + Vercel デプロイ
- Supabaseリージョン: Tokyo (ap-northeast-1)
- 通信はHTTPS(Vercel/Supabase標準)、保存時暗号化はSupabase標準(AES-256)。追加実装不要だがREADMEに明記すること
- UIは既存プロトタイプのデザイン(teal #0F9B8E × orange #E8792B、Meiryo系フォント、カードUI)を踏襲。スマホ・PC両対応を維持
- 調査票は厚労省「職業性ストレス簡易調査票(57項目)」、判定は合計点数法(B≧77、またはB≧63かつA+C≧76)。既存プロトタイプのスコアリングロジックをそのまま移植

## 1. ロールと閲覧範囲(スキーマと一致させること)

| ロール | 想定者 | 個人結果 | 集団分析 | 面接指導申出 | アクセスログ |
|---|---|---|---|---|---|
| office | 産業医事務所 | 全企業・全結果 | 全企業 | 全企業分を閲覧・状態更新 | 閲覧可 |
| jimu | 実施事務従事者(企業ごと) | 自社の全結果 ※誓約必須 | 自社 | 自社分を閲覧・状態更新 | 不可 |
| company | 事業者側担当者 | 自社の同意ありのみ | 自社 | 不可 | 不可 |
| employee | 従業員 | 自分のみ | 不可 | 自分の申出のみ | 不可 |

- jimuは初回ログイン時に「私は人事権(解雇・昇進・異動の決定権)を有しません」の誓約画面を表示し、同意で `profiles.no_personnel_authority = true, attested_at = now()` を更新。誓約するまで結果画面はRLSにより空になるため、未誓約時は誓約画面へ誘導すること
- 企業間分離はRLSが保証するが、UI上も他社情報が混ざらないよう company_id でフィルタした表示にすること

## 2. 認証(個別アカウント)

- Supabase Auth のメール+パスワード方式。office がユーザーを招待(inviteUserByEmail)し、profiles にロール・企業・部署を登録する管理画面を作る
- 従業員の一括登録: office画面にCSVアップロード(氏名, メール, 社員番号, 部署, 企業コード)→ Supabase Admin APIで一括招待。※Admin APIはservice_roleキーが必要なので、Vercelのサーバーレス関数(/api/invite)経由で実行し、キーはフロントに露出させないこと
- パスワードリセットはSupabase標準フローを有効化

## 3. 画面一覧

1. **ログイン** / パスワード再設定
2. **従業員**: 受検(57問・年度重複時はエラー表示)→ 結果表示 → 同意チェック(後から結果画面で同意の追加・撤回可) → **面接指導申出フォーム**(高ストレス判定時に表示。連絡事項・希望日時を入力→ interview_requests へINSERT)→ 自分の申出状況の確認
3. **office ダッシュボード**: 企業切替タブ → 年度選択 → 結果一覧(高ストレスフィルタ)/ 面接指導申出一覧(status更新: pending→scheduled→done)/ 集団分析 / **CSV出力** / ユーザー管理(招待・ロール付与)/ アクセスログ閲覧
4. **jimu ダッシュボード**: 誓約画面 → 自社の結果一覧 / 申出一覧(状態更新可)/ 集団分析 / CSV出力(自社分)
5. **company ダッシュボード**: 自社の集団分析 + 同意あり個人結果一覧のみ。申出情報は表示しない

## 4. 機能仕様の詳細

### 4.1 面接指導申出と通知
- 申出INSERT後、その企業の jimu 全員と office 全員へメール通知
- 実装: Supabase Database Webhook(interview_requests INSERT)→ Edge Function `notify-interview` → Resend API でメール送信(Resendの無料枠で開始。APIキーはEdge Functionのsecretsに保存)
- メール本文には個人名・スコアを書かない(「面接指導の申出が1件あります。システムにログインして確認してください」+企業名のみ)。メールは平文経路がありうるため要配慮個人情報を載せない
- 通知先メールアドレスは profiles から取得(office全員 + 該当company_idのjimu)

### 4.2 集団分析
- RPC `group_analysis(company_id, fiscal_year)` を呼ぶだけ。10名未満の部署はDB側で除外済み
- 表示: 部署別の受検者数 / 高ストレス者数 / 高ストレス率 / A・B・C平均点の表 + 高ストレス率の横棒グラフ(recharts)
- 「10名未満の部署は個人特定防止のため表示されません」の注記を必ず表示

### 4.3 CSV出力
- office / jimu のみ。ボタン押下でクライアント側生成(BOM付きUTF-8、Excelで文字化けしないこと)
- 列: 実施日, 年度, 氏名, 社員番号, 部署, A, B, C, D, 高ストレス判定, 同意有無
- 出力時に `log_access('export_csv', '企業名/年度', company_id)` を必ず呼ぶ

### 4.4 アクセスログ
- 次のタイミングで `log_access()` RPCを呼ぶ: 結果一覧の表示、個人結果詳細の表示、CSV出力、申出一覧の表示、ユーザー招待
- officeダッシュボードに日時・ユーザー・ロール・操作・対象の一覧(日付フィルタ付き)を表示。ログは削除不可(RLSでポリシー未定義)

### 4.5 受検制御
- fiscal_year は受検時にシステム側で決定(例: 4月始まり年度)。unique制約違反時は「本年度は受検済みです」と案内
- office画面に企業ごとの「実施期間」設定(companies拡張 or 別テーブル)を追加し、期間外は受検ボタンを無効化(任意・余力があれば)

## 5. セキュリティ実装チェックリスト

- [ ] service_role キーは絶対にフロントに置かない(Vercelサーバーレス関数の環境変数のみ)
- [ ] anon キーで動くのはRLS前提の操作のみ
- [ ] 結果(results)のUPDATE/DELETEはRLSで不可(consent列のみ本人更新可)になっていることをテストで確認
- [ ] 企業Aのjimu/companyアカウントで企業Bのデータが1行も取得できないことの自動テストを書く
- [ ] 未誓約jimuが結果を取得できないことを確認
- [ ] メール通知に個人情報を含めない
- [ ] READMEに: 通信TLS・保存時暗号化・結果5年保存(削除しない運用)・実施事務従事者の選任は衛生委員会審議事項である旨を記載

## 6. デプロイ

- GitHub main → Vercel 自動デプロイ。環境変数: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY(サーバー側のみ), RESEND_API_KEY(Edge Function secrets)
- 独自ドメイン(例: check.mestate.jp)をVercelに設定する手順をREADMEに記載

## 7. 受け入れテスト(最後に必ず実施)

1. 従業員A(企業X)が受検→本人に結果表示、同年度2回目はブロック
2. 高ストレスの従業員Aが申出→企業XのjimuとofficeにメールA着信、企業Yのjimuには届かない
3. 企業Xのjimu(誓約済)は企業Xの全結果を見え、企業Yの結果は0件
4. 企業Xのcompanyは同意あり結果のみ+集団分析のみ
5. 9名の部署は集団分析に出ず、10名の部署は出る
6. CSV出力がExcelで文字化けしない/出力操作がアクセスログに残る
