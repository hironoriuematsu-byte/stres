-- ============================================================
-- 0004: 従業員の自己入力方式への変更
--
-- 氏名・社員番号・部署は招待時に管理者が設定するのではなく、
-- 従業員本人が受検時に入力する運用に変更。
-- 0003で列レベル権限に name / dept は含めたが emp_id が
-- 含まれていなかったため追加する。
-- ============================================================

grant update (emp_id) on table public.profiles to authenticated;
