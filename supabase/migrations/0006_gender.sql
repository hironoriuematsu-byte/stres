-- ============================================================
-- 0006: 個人結果票(素点換算)対応
--
-- 厚労省の素点換算表は男女で評価基準が異なるため、
-- 受検時に性別を記録する(既存データはNULL=素点換算なし表示)。
-- ============================================================

alter table public.results
  add column if not exists gender text check (gender in ('male', 'female'));
